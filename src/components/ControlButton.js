import React from 'react';

import './../App.css';

class ControlButton extends React.Component {
	render = () => {
		return (
			<div className="">
				<button>SET</button>
			</div>

		);
	}
}

export default ControlButton;
